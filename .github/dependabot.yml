version: 2
updates:
  # Keep the pinned action SHAs in .github/workflows/ current. Dependabot
  # proposes a PR per new release; CI must pass before merge.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    open-pull-requests-limit: 5
